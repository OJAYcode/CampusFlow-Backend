<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Student Sharing Request - UniTrack</title>
    <style>
      * { margin: 0; padding: 0; box-sizing: border-box; }

      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
        background: #f5f5f7;
        padding: 20px;
        color: #1d1d1f;
      }

      .email-wrapper {
        max-width: 600px;
        margin: 40px auto;
        background: #ffffff;
        border-radius: 20px;
        overflow: hidden;
        border: 1px solid #e5e5ea;
        box-shadow: 0 8px 30px rgba(0,0,0,0.06);
      }

      .header {
        padding: 35px 30px 20px;
        text-align: center;
        border-bottom: 1px solid #f0f0f0;
      }

      .header h1 {
        font-size: 1.8rem;
        font-weight: 600;
        color: #111827;
        margin-bottom: 6px;
      }

      .subtitle {
        font-size: 0.95rem;
        color: #6b7280;
        font-weight: 400;
      }

      .content {
        padding: 40px 30px;
      }

      .greeting {
        font-size: 1.2rem;
        font-weight: 600;
        margin-bottom: 20px;
      }

      .message {
        font-size: 1rem;
        color: #4b5563;
        line-height: 1.6;
        margin-bottom: 30px;
      }

      .request-details {
        background: #fafafa;
        border: 1px solid #e5e7eb;
        border-radius: 14px;
        padding: 20px;
        margin-bottom: 25px;
      }

      .detail-row {
        display: flex;
        justify-content: space-between;
        padding: 10px 0;
        border-bottom: 1px solid #f0f0f0;
        font-size: 0.95rem;
      }

      .detail-row:last-child {
        border-bottom: none;
      }

      .detail-label {
        font-weight: 500;
        color: #374151;
      }

      .detail-value {
        font-weight: 600;
        color: #111827;
      }

      .user-message {
        margin: 20px 0;
        padding: 15px 20px;
        border: 1px solid #fcd34d;
        background: #fffbeb;
        border-radius: 12px;
        font-size: 0.95rem;
        color: #92400e;
      }

      .action-buttons {
        text-align: center;
        margin: 30px 0;
      }

      .btn {
        display: inline-block;
        padding: 14px 28px;
        border-radius: 10px;
        text-decoration: none;
        font-size: 1rem;
        font-weight: 600;
        background: #4f46e5;
        color: #ffffff;
        border: 1px solid #4338ca;
        transition: background 0.2s ease;
      }

      .btn:hover {
        background: #4338ca;
      }

      .footer {
        text-align: center;
        padding: 25px;
        border-top: 1px solid #f0f0f0;
        background: #fafafa;
        font-size: 0.85rem;
        color: #6b7280;
      }

      .timestamp {
        margin-top: 10px;
        font-size: 0.8rem;
        color: #9ca3af;
      }
    </style>
  </head>
  <body>
    <div class="email-wrapper">
      <div class="header">
        <h1>🎓 UniTrack</h1>
        <div class="subtitle">Student Sharing Request</div>
      </div>

      <div class="content">
        <div class="greeting">
          Hello {{targetTeacherName}} 👋
        </div>

        <div class="message">
          You have received a student sharing request from
          <strong>{{requesterName}}</strong>. They want to add some of your students to their course.
        </div>

        <div class="request-details">
          <div class="detail-row">
            <div class="detail-label">👨‍🏫 Requesting Teacher</div>
            <div class="detail-value">{{requesterName}}</div>
          </div>
          <div class="detail-row">
            <div class="detail-label">📚 Their Course</div>
            <div class="detail-value">{{requesterCourse}}</div>
          </div>
          <div class="detail-row">
            <div class="detail-label">📖 Your Course</div>
            <div class="detail-value">{{targetCourse}}</div>
          </div>
          <div class="detail-row">
            <div class="detail-label">👥 Students</div>
            <div class="detail-value">{{studentCount}} student{{#if_gt studentCount 1}}s{{/if_gt}}</div>
          </div>
        </div>

        {{#if message}}
          <div class="user-message">
            💬 Message from {{requesterName}}:<br />
            "{{message}}"
          </div>
        {{/if}}

        <div class="action-buttons">
          <a href="{{approveUrl}}" class="btn">📋 Review Request</a>
        </div>

        <div class="message">
          Please log in to UniTrack to review the student details and respond to this request.
        </div>
      </div>

      <div class="footer">
        <p>This is an automated message from UniTrack Attendance System.</p>
        <p>If you did not expect this email, contact your system administrator.</p>
        <div class="timestamp">
          Request ID: {{requestId}} | {{timestamp}}
        </div>
      </div>
    </div>
  </body>
</html>
